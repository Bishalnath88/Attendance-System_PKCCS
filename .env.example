# Flask configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=0
SESSION_TTL_HOURS=12

# Backend API host for frontend runtime config
API_BASE_URL=http://localhost:5000

# Production Firebase secret (paste the full service account JSON string here in your host secrets)
GOOGLE_SERVICE_ACCOUNT_JSON=
